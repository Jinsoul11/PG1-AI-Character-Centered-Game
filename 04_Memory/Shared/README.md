# Memory / Shared

共同经历（Shared Experience）：仅 GameSystem / Spine 框架写入，LLM 不可新增。

- `RemiSharedExperienceMemory` — 持久化存储
- `RemiSharedExperienceCatalog` — id / frame / Ending 页定义
- `RemiSharedExperienceTypes` / EndingCatalog
