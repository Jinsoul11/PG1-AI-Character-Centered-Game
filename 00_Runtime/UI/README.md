# Runtime / UI

面板与 UI 栈（原 `Panel/` + `Manager/UiManager`）。

含：`BasePanel`、`DialoguePanel`、`PhoneApp*`、`StoryPanel`、`StartPanel`、`SceneTravelPanel`、`CheckPanel`、Ema 对话面板、Social 列表行等。

预制体仍在 `Resources/UI/`（不在本目录）。
