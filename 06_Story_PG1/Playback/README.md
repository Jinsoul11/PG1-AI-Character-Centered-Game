# 06_Story_PG1 / Playback

**通用剧情演出壳**（原顶层 `Story/`）。

- `StoryDirector` / 黑屏旁白 / 叙事 Hint / Trigger 开场
- 负责「怎么播」，不承载 PG1 主线剧情数据

PG1 主线内容在 `../Spine/`；可选事件在 `../Optional/`。  
面板 `StoryPanel` 仍在 `../../00_Runtime/UI/`。
